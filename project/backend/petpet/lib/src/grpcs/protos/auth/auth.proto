syntax = "proto3";

message Pets{
    int32 id = 1;
    string name = 2;
    string password = 3;
    string name = 4;
}

// TODO: посмотреть, как можно делать экземпляры других сущностей внутри первой
// хочу сделать вариант User{
//                          AuthUser user = N;    
// }
// чтобы можно было создавать сущности пользователей и хранить в них 
// их же авторизованные версии